# Modelos

Esta carpeta guarda metadatos de configuracion y entrenamiento usados para
reproducir los pipelines.

No se incluyen modelos binarios pesados. Si se ejecuta el pipeline desde cero,
los notebooks vuelven a entrenar y regenerar los artefactos necesarios.
